"use strict";

function generateField(size = 0){
    const field = document.getElementById('field');
    let counter = 0;
    for(let i = size*size; i > 0; i --){

        let cell = document.createElement(div);
        cell.setAttribute('cell', ``)

    }
    console.log(counter);
    return null;
}

console.log("Connected");
generateField(2);